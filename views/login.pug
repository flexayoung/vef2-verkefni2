extends layout

block content
  if failureFlash
    p failureFlash
  div.form-wrapper
    form(method='POST' action='/login')
      div.form-group
        label.form-label(for='name') Notendanafn:
        input.form-control(type='text' name='username' value=username)
      div.form-group
        label.form-label(for='password') Lykilorð:
        input.form-control(type='password' name='password' value=password)
    
      button.button(type='submit') Senda
  
block foot
  a.link(href='/') Forsíða

        
        

    
